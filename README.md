
  # High-Fidelity LMS Design System

  This is a code bundle for High-Fidelity LMS Design System. The original project is available at https://www.figma.com/design/88l0C1viC13oD25zFSl84x/High-Fidelity-LMS-Design-System.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  